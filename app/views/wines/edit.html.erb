<div class="row spacer">
	<div class="col-xs-8"><h1>Update Wine</h1></div>
</div>

<%= simple_form_for(@wine, url: { action: :update }, html: { class: "form-horizontal" }) do |f| %>
	<%= render partial: "form", locals: { :f => f } %>
	<%= f.button :submit, class: "col-xs-offset-2 spacer" %>
<% end %>