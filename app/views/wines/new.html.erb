<div class="row">
	<div class="col-xs-8"><h1>New Wine</h1></div>
</div>

<%= simple_form_for @wine, url: { action: :create }, html: { class: "form-horizontal" } do |f| %>
	<%= render partial: "form", locals: { :f => f } %>
	<%= f.button :submit %>
<% end %>