<div class='row spacer'>
	<div class='col-xs-12'>
		<h1><%= "New #{@klass.to_s}" %></h1>
	</div>
</div>

<%= simple_form_for(@firm, url: {action: :create}, html: { class: 'form-horizontal' }) do |f| %>	
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>