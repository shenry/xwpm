	<div class='form-group'>
		<span class='col-xs-2 control-label bold'>Product Image</span>
			<% if object.image? %>
				<%= render partial: '/partials/avatar_thumbnail', locals: { :object => object } %>
			<% end %>
			<div class='col-xs-2'>
				<%= f.file_field image %>
			</div>
	</div>